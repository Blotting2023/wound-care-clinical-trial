# 创面评估系统 V1 开发需求规格说明书（修订版）

版本：V1.0-revised  
日期：2026-05-17  
用途：本文件用于指导 Hermes Agent 进行系统架构、数据库、后端 API、移动端 App 与 Web 管理端的代码实现。

## 1. V1 产品边界

### 1.1 产品形态

V1 仅开发原生移动 App 与 Web 管理端，不开发微信小程序。

- 移动端：Flutter 原生 App，支持 iOS 与 Android。
- Web 管理端：Vue3 + TypeScript。
- 后端：Java 21 + Spring Boot 3.x。
- 数据库：PostgreSQL 15+。
- 文件存储：兼容 S3 协议的对象存储，私有化部署时可使用 MinIO。
- 部署：Docker Compose 为 V1 标准交付形态，预留 Kubernetes 部署配置。

### 1.2 V1 必须完成

- 用户、组织、科室/中心、角色与权限管理。
- 患者/受试者档案管理。
- 创面档案管理。
- 移动端标准化图像采集、校准卡识别、隐私打码、离线草稿。
- 图像上传与云端 AI 分析任务。
- AI 结果展示、人工校正、结构化量表录入。
- 评估记录锁定提交与电子签名。
- 稽查轨迹、访问日志、签名日志。
- Web 管理端病程查看、评估记录审核、基础导出。
- 符合 GCP、PIPL 与 21 CFR Part 11 的基础数据设计。

### 1.3 V1 暂不完成但必须预留

- 具体医院 HIS/EMR 硬编码对接。
- 微信小程序。
- 多语言。
- 复杂统计分析报表。
- AI 模型训练平台。
- 自动生成医学诊断或治疗建议。
- 正式 Kubernetes 生产集群编排。

## 2. 术语与核心对象

- 患者 Patient：真实医疗场景中的病人主体，包含 PHI。
- 受试者 Subject：临床试验中的入组对象。一个 Patient 可对应多个 Subject。
- 创面 Wound：患者身上的一个具体创面，具备解剖部位、侧别、首次发现日期等信息。
- 评估记录 Assessment：一次对某个创面的图像、AI、人工校正、量表与签名数据的完整记录。
- AI 结果：CV 服务返回的机器估计数据，不作为最终事实。
- 最终结果：医师或授权护理人员确认后的数据，作为临床事实数据。
- 锁定：评估记录进入不可直接修改状态。
- 修订：锁定后的记录如需修改，必须创建修订版本，不允许原地覆盖。

## 3. 角色与权限

### 3.1 内置角色

系统必须内置以下角色，支持后续自定义角色扩展。

| 角色 | 说明 |
| --- | --- |
| SYSTEM_ADMIN | 系统管理员，管理租户、系统配置、全局字典，不直接修改临床数据 |
| ORG_ADMIN | 机构管理员，管理本机构用户、科室、权限 |
| STUDY_ADMIN | 研究项目管理员，管理项目、中心、受试者入组 |
| PHYSICIAN | 医师，可确认评估结果并电子签名 |
| NURSE | 护士，可采集图像、创建草稿、提交待确认记录 |
| DATA_MANAGER | 数据管理员，可查看、质控、导出授权范围内数据 |
| CRO_MONITOR | CRO 监察员，只读查看授权项目数据与稽查轨迹 |
| AUDITOR | 审计员，只读查看签名、稽查、访问日志 |

### 3.2 权限原则

- 所有用户必须归属一个机构。
- 临床数据按机构、科室/中心、研究项目三层隔离。
- 默认最小权限。
- 跨机构访问默认禁止。
- 系统管理员不得绕过业务权限直接修改临床数据。
- 所有临床数据导出必须记录访问日志。

### 3.3 关键权限矩阵

| 功能 | NURSE | PHYSICIAN | DATA_MANAGER | CRO_MONITOR | ORG_ADMIN | AUDITOR |
| --- | --- | --- | --- | --- | --- | --- |
| 创建患者/受试者 | 是 | 是 | 是 | 否 | 是 | 否 |
| 创建创面 | 是 | 是 | 否 | 否 | 否 | 否 |
| 创建评估草稿 | 是 | 是 | 否 | 否 | 否 | 否 |
| 上传图片 | 是 | 是 | 否 | 否 | 否 | 否 |
| 发起 AI 分析 | 是 | 是 | 否 | 否 | 否 | 否 |
| 人工校正 | 是 | 是 | 否 | 否 | 否 | 否 |
| 锁定并签名 | 否 | 是 | 否 | 否 | 否 | 否 |
| 查看病程 | 授权范围 | 授权范围 | 授权范围 | 授权范围 | 授权范围 | 授权范围 |
| 导出报表 | 否 | 是 | 是 | 是 | 是 | 是 |
| 查看 audit log | 否 | 否 | 是 | 是 | 是 | 是 |

## 4. 评估记录状态机

Assessment 必须实现明确状态机，不允许任意跳转。

| 状态 | 含义 | 可执行动作 |
| --- | --- | --- |
| DRAFT | 本地或云端草稿，未完成 | 编辑、删除、上传图片 |
| UPLOADED | 图片已上传，待 AI 分析 | 发起 AI、编辑基础信息 |
| AI_PROCESSING | AI 分析中 | 查询任务状态、取消任务 |
| AI_FAILED | AI 失败 | 重试 AI、跳过 AI 进入人工评估 |
| AI_COMPLETED | AI 完成，待人工确认 | 人工校正、填写量表 |
| PENDING_SIGNATURE | 已完成内容，待签名 | 签名、退回草稿 |
| LOCKED | 已锁定并签名 | 查看、发起修订 |
| REVISION_DRAFT | 锁定记录的修订草稿 | 编辑、提交修订签名 |
| VOIDED | 作废 | 只读查看 |

状态跳转规则：

- DRAFT -> UPLOADED：至少存在一张合格图像。
- UPLOADED -> AI_PROCESSING：网络在线，图片已完成隐私脱敏。
- AI_PROCESSING -> AI_COMPLETED：CV 服务返回合法 JSON 多边形。
- AI_PROCESSING -> AI_FAILED：CV 服务超时或失败。
- AI_FAILED -> PENDING_SIGNATURE：允许人工跳过 AI，但必须填写失败原因。
- AI_COMPLETED -> PENDING_SIGNATURE：人工确认所有必填项。
- PENDING_SIGNATURE -> LOCKED：电子签名成功，事务提交成功。
- LOCKED -> REVISION_DRAFT：必须填写修订原因。
- LOCKED -> VOIDED：仅 PHYSICIAN 或 DATA_MANAGER 在授权范围内可操作，必须填写作废原因并签名。

## 5. 移动端 App 需求

### 5.1 登录与会话

- 支持账号密码登录。
- 登录成功后服务端签发 access token 与 refresh token。
- 移动端本地仅保存加密 token，必须使用系统安全存储：
  - iOS：Keychain。
  - Android：Keystore。
- App 15 分钟无操作后锁屏。
- 解锁方式：
  - 首选 FaceID/TouchID/Android Biometric。
  - 备用方式为本地 PIN。
- 离线登录仅允许使用未过期的本地 token 与生物识别/PIN 解锁。
- 离线登录后只允许访问已缓存数据与本地草稿。

### 5.2 离线边界

离线允许：

- 查看已缓存患者/受试者列表。
- 查看已缓存创面与历史摘要。
- 新建本地评估草稿。
- 使用相机采集图像。
- 本地隐私打码。
- 本地填写结构化量表。

离线禁止：

- 拉取最新 HIS/EMR 数据。
- 上传图片。
- 发起 AI 分析。
- 电子签名。
- 锁定提交。
- 导出临床报表。

### 5.3 离线同步策略

- 本地草稿必须使用 UUID 作为 local_id。
- 云端记录使用 server_id。
- 每条云端临床记录必须有 row_version 整数版本号。
- App 同步时必须提交 base_row_version。
- 若 base_row_version 与云端当前版本不一致，不允许静默覆盖。
- 冲突记录进入 SYNC_CONFLICT 状态，由用户人工选择：
  - 保留云端版本。
  - 以本地内容创建新修订草稿。
  - 放弃本地草稿。
- 所有冲突与处理动作写入 audit_log。

### 5.4 图像采集

App 必须使用 Flutter 调用原生相机能力，必要时实现 iOS/Android platform channel。

必须支持：

- 触摸对焦。
- 锁定白平衡。
- 锁定曝光补偿 EV。
- 拍照前识别校准卡。
- 拍照前检查校准卡完整在画面内。
- 拍照前检查图像清晰度，模糊时提示重拍。
- 拍照后允许重拍。

校准卡要求：

- 物理尺寸：5cm x 5cm。
- 包含标准 RGB 色块。
- 包含定位二维码。
- 二维码内容格式：`CALCARD:{card_id}:{version}:{size_mm}`。
- V1 size_mm 固定为 50。
- App 识别二维码与四角定位点后，计算 scale_factor_mm_per_pixel。
- 若无法识别校准卡，不允许进入正式采集，只能保存为非合格草稿图片。

图像压缩：

- 原始拍照文件不上传云端，除非部署配置明确允许。
- 上传图长边限制为 1920px。
- JPEG quality = 85。
- 必须移除 EXIF GPS 与设备唯一标识。
- 单张目标大小约 1MB，最大不得超过 5MB。

### 5.5 图像隐私脱敏

- 上传前必须进入脱敏确认步骤。
- App 提供矩形/画笔打码工具。
- AI 自动识别脱敏区域可作为辅助，但用户必须可手动调整。
- 脱敏为不可逆模糊或像素化。
- 云端仅接收脱敏后图片。
- 如用户标记“图片无隐私区域”，也必须记录该确认动作。

### 5.6 AI 分析与人工校正

CV 服务返回格式必须为 JSON，不允许返回栅格 mask 图片作为主结果。

AI 返回数据至少包含：

- wound_polygon：创面边缘多边形坐标。
- tissue_polygons：组织类型多边形数组。
- area_mm2。
- length_mm。
- width_mm。
- tissue_percentages：
  - granulation_red_percent。
  - slough_yellow_percent。
  - necrosis_black_percent。
- confidence。
- model_name。
- model_version。

坐标规范：

- 坐标以脱敏后上传图像为参考系。
- 坐标单位为像素。
- 多边形点格式：`[{ "x": 123.4, "y": 567.8 }]`。
- 数据库存储时必须同时保存：
  - image_width_px。
  - image_height_px。
  - scale_factor_mm_per_pixel。
  - ai_polygon_json。
  - final_polygon_json。

人工校正规则：

- App 使用 Flutter Canvas 展示 AI 多边形。
- 用户可拖拽锚点、增加点、删除点。
- 人工确认后的 final_* 字段作为最终事实数据。
- AI 原始结果不得被覆盖。
- 若用户未修改 AI 结果，也必须复制到 final_* 字段并记录 confirmed_without_change = true。

## 6. 结构化临床量表

### 6.1 疼痛

- NRS：整数 0-10，必填。
- VAS：0-100 滑动条，选填。
- 若患者无法配合评分，记录 unable_to_score = true，并填写原因。

### 6.2 温哥华瘢痕量表 VSS

VSS 必须结构化存储，不允许只保存总分。

字段：

- vascularity_score。
- pigmentation_score。
- pliability_score。
- height_score。
- total_score 自动计算。

### 6.3 窦道与潜行

- 支持多条记录。
- 每条包含：
  - clock_direction：1-12。
  - depth_cm：decimal(5,2)。
  - note：可选。

## 7. Web 管理端需求

### 7.1 页面范围

V1 Web 必须实现：

- 登录页。
- 用户管理。
- 角色权限管理。
- 机构/科室/中心管理。
- 患者/受试者列表。
- 创面详情页。
- 评估记录详情页。
- 病程时间轴。
- AI 与人工结果对比视图。
- 电子签名记录页。
- 稽查轨迹查询页。
- 基础报表导出页。

### 7.2 病程可视化

创面详情页必须展示：

- 按时间排序的评估记录。
- 面积变化曲线。
- 长度/宽度变化曲线。
- 组织占比堆叠图。
- 历次图片缩略图。
- 每次记录的状态、签名人、签名时间。

### 7.3 导出

V1 支持导出 CSV 与 PDF。

导出内容：

- 患者/受试者基础信息。
- 创面基础信息。
- 历次评估指标。
- 签名信息。
- AI 模型版本。
- 人工修订记录摘要。

导出必须：

- 校验权限。
- 写入 access_log。
- 生成导出任务记录。
- 导出文件默认 24 小时后过期。

## 8. 后端 API 设计要求

### 8.1 通用要求

- API 使用 RESTful 风格。
- 请求与响应使用 JSON。
- 所有时间使用 UTC ISO-8601。
- 所有金额/测量 decimal 不使用浮点存储。
- 所有修改临床数据的 API 必须要求 change_reason。
- 所有分页接口必须支持 page、size、sort。
- 所有列表查询必须按当前用户权限过滤。

### 8.2 必需 API 模块

认证：

- `POST /api/auth/login`
- `POST /api/auth/refresh`
- `POST /api/auth/logout`
- `POST /api/auth/unlock`
- `POST /api/auth/signature-pin/setup`
- `POST /api/auth/signature-pin/verify`

患者/受试者：

- `GET /api/patients`
- `POST /api/patients`
- `GET /api/patients/{id}`
- `PUT /api/patients/{id}`
- `POST /api/subjects`
- `GET /api/subjects`
- `GET /api/subjects/{id}`
- `POST /api/integration/lookup-by-code`

创面：

- `POST /api/wounds`
- `GET /api/wounds/{id}`
- `PUT /api/wounds/{id}`
- `GET /api/patients/{patientId}/wounds`

评估：

- `POST /api/assessments`
- `GET /api/assessments/{id}`
- `PUT /api/assessments/{id}`
- `POST /api/assessments/{id}/images`
- `POST /api/assessments/{id}/ai-analysis`
- `GET /api/assessments/{id}/ai-analysis`
- `POST /api/assessments/{id}/manual-confirm`
- `POST /api/assessments/{id}/submit-for-signature`
- `POST /api/assessments/{id}/lock`
- `POST /api/assessments/{id}/revision`
- `POST /api/assessments/{id}/void`

同步：

- `POST /api/sync/push`
- `GET /api/sync/pull`
- `POST /api/sync/conflicts/{id}/resolve`

审计：

- `GET /api/audit-logs`
- `GET /api/access-logs`
- `GET /api/signature-logs`

报表：

- `POST /api/exports`
- `GET /api/exports/{id}`
- `GET /api/exports/{id}/download`

## 9. 数据库设计要求

Hermes Agent 必须优先完成 PostgreSQL DDL 与迁移脚本。推荐使用 Flyway。

### 9.1 必需表

组织与权限：

- tenant。
- organization。
- department。
- user_account。
- role。
- permission。
- user_role。
- role_permission。

临床主体：

- patient。
- subject。
- study。
- study_site。
- wound。
- assessment_record。
- assessment_image。
- assessment_tunnel。
- assessment_scale。

合规日志：

- audit_log。
- access_log。
- signature_log。
- export_job。

AI 与同步：

- ai_analysis_job。
- sync_conflict。

### 9.2 patient 表关键字段

PHI 字段必须加密存储。需要检索的 PHI 字段同时保存 hash 字段。

必需字段：

- id UUID PK。
- organization_id UUID。
- encrypted_name bytea。
- name_hash varchar(128)。
- encrypted_medical_record_no bytea。
- medical_record_no_hash varchar(128)。
- encrypted_id_number bytea。
- id_number_hash varchar(128)。
- encrypted_phone bytea。
- phone_hash varchar(128)。
- gender varchar(20)。
- birth_date date。
- created_at timestamptz。
- updated_at timestamptz。
- row_version int。
- deleted_at timestamptz nullable。

### 9.3 assessment_record 表关键字段

必须清晰区分 AI 字段与最终确认字段。

必需字段：

- id UUID PK。
- organization_id UUID。
- patient_id UUID。
- subject_id UUID nullable。
- wound_id UUID。
- status varchar(40)。
- assessment_time timestamptz。
- image_width_px int。
- image_height_px int。
- scale_factor_mm_per_pixel numeric(12,8)。
- calibration_card_id varchar(100)。
- ai_area_mm2 numeric(12,2) nullable。
- ai_length_mm numeric(12,2) nullable。
- ai_width_mm numeric(12,2) nullable。
- ai_polygon_json jsonb nullable。
- ai_tissue_json jsonb nullable。
- ai_confidence numeric(5,4) nullable。
- ai_model_name varchar(100) nullable。
- ai_model_version varchar(100) nullable。
- final_area_mm2 numeric(12,2) nullable。
- final_length_mm numeric(12,2) nullable。
- final_width_mm numeric(12,2) nullable。
- final_polygon_json jsonb nullable。
- final_tissue_json jsonb nullable。
- confirmed_without_change boolean default false。
- manual_override_reason text nullable。
- nrs_score int nullable。
- vas_score int nullable。
- unable_to_score boolean default false。
- pain_score_reason text nullable。
- vss_vascularity_score int nullable。
- vss_pigmentation_score int nullable。
- vss_pliability_score int nullable。
- vss_height_score int nullable。
- vss_total_score int nullable。
- created_by UUID。
- updated_by UUID。
- locked_by UUID nullable。
- locked_at timestamptz nullable。
- parent_assessment_id UUID nullable。
- revision_no int default 0。
- row_version int。
- created_at timestamptz。
- updated_at timestamptz。
- deleted_at timestamptz nullable。

约束：

- status 必须使用枚举或 check constraint。
- final_area_mm2 在 LOCKED 状态必须非空。
- locked_at 在 LOCKED 状态必须非空。
- nrs_score 必须在 0-10。
- vas_score 必须在 0-100。
- scale_factor_mm_per_pixel 必须大于 0。
- row_version 每次 UPDATE 自动递增。

### 9.4 assessment_image 表关键字段

- id UUID PK。
- assessment_id UUID。
- image_type varchar(40)：ORIGINAL_LOCAL、UPLOAD_DERIVED、MASKED_UPLOAD、THUMBNAIL。
- storage_key varchar(500)。
- sha256 varchar(64)。
- width_px int。
- height_px int。
- file_size_bytes bigint。
- mime_type varchar(100)。
- privacy_mask_confirmed boolean。
- privacy_mask_json jsonb nullable。
- calibration_detected boolean。
- quality_status varchar(40)：PASSED、BLURRY、NO_CALIBRATION_CARD、MANUAL_ACCEPTED。
- created_at timestamptz。

### 9.5 audit_log 表

audit_log 必须 append-only。

必需字段：

- id UUID PK。
- table_name varchar(100)。
- record_id UUID。
- action_type varchar(20)：INSERT、UPDATE、DELETE、REVISION、VOID。
- old_value_json jsonb nullable。
- new_value_json jsonb nullable。
- changed_by_user_id UUID。
- change_reason text。
- transaction_id UUID。
- ip_address inet nullable。
- user_agent text nullable。
- created_at timestamptz。

规则：

- 不允许 UPDATE。
- 不允许 DELETE。
- 临床核心表 INSERT/UPDATE/DELETE 必须自动写入 audit_log。
- 业务 API 对临床核心表写入时必须传入 change_reason。
- SELECT 不写 audit_log，查看与导出写 access_log。

### 9.6 signature_log 表

必需字段：

- id UUID PK。
- assessment_id UUID。
- signer_user_id UUID。
- signature_method varchar(40)：PIN、SMS_OTP。
- signature_meaning varchar(200)，例如“本人确认该评估记录准确完整”。
- signed_payload_hash varchar(64)。
- signed_at timestamptz。
- ip_address inet nullable。
- user_agent text nullable。
- verification_success boolean。

电子签名事务要求：

- 锁定 assessment_record。
- 写入 signature_log。
- 写入 audit_log。
- 三者必须在同一个数据库事务内完成。
- 任一失败必须整体回滚。

### 9.7 access_log 表

用于记录查看 PHI、查看临床详情、导出数据等读行为。

字段：

- id UUID PK。
- user_id UUID。
- action_type varchar(40)：VIEW_PATIENT、VIEW_ASSESSMENT、EXPORT。
- resource_type varchar(100)。
- resource_id UUID nullable。
- request_path varchar(500)。
- ip_address inet nullable。
- user_agent text nullable。
- created_at timestamptz。

## 10. 安全与合规要求

### 10.1 PHI 加密

- PHI 必须在应用层使用 AES-256-GCM 加密后入库。
- 密钥不得写入代码仓库。
- 私有化部署时密钥通过环境变量或密钥管理服务注入。
- 加密字段不得出现在普通日志、audit_log 明文、错误堆栈中。
- 需要检索的 PHI 使用标准化后 HMAC-SHA256 hash 检索。

### 10.2 传输安全

- 所有生产环境接口必须使用 HTTPS。
- TLS 版本最低 1.2，推荐 1.3。
- 移动端不得信任无效证书。
- 上传图片必须使用短期授权 URL 或后端代理上传。

### 10.3 PIN 与短信验证码

- 签名 PIN 为独立 6 位数字，不得等同登录密码。
- PIN 只保存强 hash，不保存明文。
- 连续 5 次失败后锁定签名功能 15 分钟。
- 短信 OTP 作为可选实现，V1 必须抽象接口，允许后续接入供应商。

## 11. HIS/EMR 抽象接口

V1 不对接具体医院系统，但必须实现抽象接口。

扫码内容：

- 病区腕带码、病历号、受试者编号均通过统一接口处理。
- App 传入 code 与 code_type。
- 后端调用 IntegrationAdapter。
- 默认实现为本系统数据库查询。

接口返回：

- patient_id。
- subject_id nullable。
- display_name_masked。
- gender。
- age。
- department。
- bed_no nullable。
- latest_updated_at。

若无网络：

- 不允许调用远程 HIS。
- 仅允许在本地缓存中查询。
- 查询结果必须标记 cache_stale = true。

## 12. 非功能需求

### 12.1 性能

- 500 个移动端并发连接。
- 患者列表 P95 < 1.5s。
- 评估提交 P95 < 1.5s，不包含图片上传与 AI 推理。
- 图片上传 + CV 推理 + JSON 返回常规网络目标 5-8s。
- AI 硬超时 20s。

### 12.2 可用性

- V1 生产目标 SLA 99.9%。
- PostgreSQL 每日全量备份。
- 对象存储每日增量备份。
- 关键配置必须支持私有化部署。

### 12.3 可观测性

- 后端必须输出结构化日志。
- 每个请求必须有 request_id。
- AI 分析任务必须记录耗时、失败原因、模型版本。
- 评估锁定失败必须记录错误原因但不得泄露 PHI。

## 13. 验收标准

### 13.1 一次完整评估

验收路径：

1. 护士登录 App。
2. 选择患者与创面。
3. 拍照时识别校准卡。
4. 完成隐私打码确认。
5. 上传图片。
6. 发起 AI 分析。
7. App 展示 AI 多边形与面积。
8. 用户拖动锚点进行人工校正。
9. 填写 NRS、VSS、窦道/潜行。
10. 提交给医师签名。
11. 医师输入签名 PIN。
12. 记录进入 LOCKED。
13. Web 端能查看病程曲线、图片、AI 与人工结果、签名日志、audit_log。

### 13.2 离线草稿

验收路径：

1. App 断网。
2. 用户用本地 token + 生物识别解锁。
3. 创建评估草稿。
4. 拍照并本地保存。
5. 尝试 AI 分析时提示需要联网。
6. 网络恢复后同步草稿。
7. 云端生成评估记录。
8. 同步过程写入 audit_log。

### 13.3 冲突处理

验收路径：

1. App 离线编辑基于 row_version = 1 的草稿。
2. Web 端将同一记录更新到 row_version = 2。
3. App 恢复联网后同步。
4. 后端拒绝静默覆盖。
5. App 展示冲突。
6. 用户选择创建修订草稿。
7. 后端生成 REVISION_DRAFT，并写入 sync_conflict 与 audit_log。

### 13.4 电子签名事务

验收路径：

1. 模拟 signature_log 写入失败。
2. 调用锁定接口。
3. assessment_record 不得进入 LOCKED。
4. audit_log 不得出现不完整锁定记录。
5. API 返回事务失败错误。

## 14. Hermes Agent 实施指令

Hermes Agent 必须按以下顺序开发，不得先写 UI 再补数据模型。

### 14.1 第一阶段：数据库与后端基础

交付物：

- Flyway migration SQL。
- PostgreSQL DDL。
- audit_log append-only 保护。
- row_version 自动递增机制。
- Spring Boot 项目骨架。
- Entity、Repository、Service、Controller。
- RBAC 权限模型。
- PHI 加密工具类与字段转换。
- 基础认证与 JWT。

优先实现表：

- organization。
- department。
- user_account。
- role。
- permission。
- patient。
- subject。
- wound。
- assessment_record。
- assessment_image。
- audit_log。
- signature_log。
- access_log。

### 14.2 第二阶段：评估主流程 API

交付物：

- 创建/更新患者。
- 创建/更新创面。
- 创建评估草稿。
- 上传图片元数据。
- 发起 AI 任务接口。
- 保存 AI 结果接口。
- 人工确认接口。
- 提交待签名接口。
- 锁定并签名接口。
- 修订接口。

事务要求：

- `lockAssessment` 方法必须使用 `@Transactional`。
- 事务内必须完成：
  - 校验签名 PIN。
  - 更新 assessment_record 状态为 LOCKED。
  - 写入 signature_log。
  - 写入 audit_log。
- 失败必须抛出异常并回滚。

### 14.3 第三阶段：移动端 App

交付物：

- Flutter 项目。
- 登录/锁屏/生物识别。
- 患者列表缓存。
- 创面列表。
- 拍照与校准卡识别接口封装。
- 图片压缩与 EXIF 清理。
- 隐私打码工具。
- 离线 SQLite 草稿。
- 同步队列。
- AI 结果 Canvas 展示与锚点拖拽。
- 量表表单。
- 签名提交流程。

### 14.4 第四阶段：Web 管理端

交付物：

- Vue3 管理端。
- 登录。
- 用户与权限管理。
- 患者/受试者管理。
- 创面详情。
- 评估详情。
- 病程图表。
- audit_log 查询。
- signature_log 查询。
- 导出任务。

### 14.5 代码质量要求

- 后端必须包含单元测试与集成测试。
- 关键事务必须有失败回滚测试。
- API 必须有 OpenAPI/Swagger 文档。
- 禁止把 PHI 输出到日志。
- 禁止硬编码密钥。
- 禁止用时间戳覆盖解决临床数据冲突。
- 禁止覆盖 AI 原始结果。
- 禁止锁定后原地修改评估记录。

## 15. 默认配置

V1 默认配置如下：

- Web 前端框架：Vue3 + TypeScript + Vite + Element Plus。
- 后端框架：Spring Boot 3.x。
- ORM：JPA 或 MyBatis Plus，优先选择团队更熟悉的一种；若无偏好，使用 JPA。
- 数据库迁移：Flyway。
- 本地对象存储：MinIO。
- 异步任务：Spring Task 或轻量队列；AI 任务状态必须入库。
- 图表：ECharts。
- 移动端本地库：SQLite。

## 16. 仍需业务确认但不阻塞开发的事项

以下事项不阻塞 V1 基础架构开发，但在进入正式临床试用前必须确认：

- 校准卡最终印刷版式与二维码内容。
- VSS 量表授权来源。
- 短信 OTP 供应商。
- 私有化部署医院的 TLS 证书管理方式。
- 是否需要保存原始未脱敏图片；默认不保存、不上传。
- 临床报表 PDF 模板。
- 具体 HIS/EMR 厂商接口。

